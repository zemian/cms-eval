This project is generated using `jbake -i`

## Setup

* Need to install JDK first!

1. Run `brew install jbake`
2. Run `jbake -i`
3. Run `jbake -b -s`

## Review Notes

- Supports Markdown and Asciidoc by default!
- Java based dev download is self contained! (well besides JDK)
- Dev server is fast
